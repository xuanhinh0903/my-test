import styled from "@emotion/styled";

export const Container = styled.div`
  .icon {
    width: 24px;
    height: 24px;
  }

  .menu-item {
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 4px 12px;
    cursor: pointer;
  }
`;
